services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    env_file:
      - .env
    ports:
      - "5173:5173"

volumes:
  node_modules:
